---
# Remove the installer itself from the freshly installed system. (Extra live-only
# Flubuntu files are cleaned by the shellprocess module.)
backend: apt
update_db: false
update_system: false

operations:
  - try_remove:
      - calamares
      - calamares-data
